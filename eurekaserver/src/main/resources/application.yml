server:
  port: 8761 #当前Eureka Server服务端口

eureka:
  client:
    register-with-eureka: false #是否将当前的Eureka server 服务作为客户端进行注册
    fetch-registry: false #是否获取其他的Eureka Server服务的树
    service-url:
      defaultZone: http://localhost:8761/eureka/ # 注册中心的访问地址
